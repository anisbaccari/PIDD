<!-- ============================================ -->
<!-- 3. SITEMAP.XML DYNAMIQUE -->
<!-- Créer un endpoint dans votre backend -->
<!-- routes/seoRoutes.js -->
<!-- ============================================ -->

import { Product } from '../models/Product.js'

export default async function seoRoutes(fastify, options) {
  
  // Sitemap principal
  fastify.get('/sitemap.xml', async (request, reply) => {
    reply.type('application/xml')
    
    const sitemap = `<?xml version="1.0" encoding="UTF-8"?>
<sitemapindex xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <sitemap>
    <loc>https://monshop.com/sitemap-main.xml</loc>
    <lastmod>${new Date().toISOString()}</lastmod>
  </sitemap>
  <sitemap>
    <loc>https://monshop.com/sitemap-products.xml</loc>
    <lastmod>${new Date().toISOString()}</lastmod>
  </sitemap>
  <sitemap>
    <loc>https://monshop.com/sitemap-categories.xml</loc>
    <lastmod>${new Date().toISOString()}</lastmod>
  </sitemap>
</sitemapindex>`
    
    return sitemap
  })

  // Sitemap pages principales
  fastify.get('/sitemap-main.xml', async (request, reply) => {
    reply.type('application/xml')
    
    const pages = [
      { url: '', priority: '1.0', changefreq: 'daily' },
      { url: 'products', priority: '0.9', changefreq: 'daily' },
      { url: 'about', priority: '0.7', changefreq: 'monthly' },
      { url: 'contact', priority: '0.6', changefreq: 'monthly' },
      { url: 'categories', priority: '0.8', changefreq: 'weekly' }
    ]

    const sitemap = `<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:image="http://www.google.com/schemas/sitemap-image/1.1">
${pages.map(page => `
  <url>
    <loc>https://monshop.com/${page.url}</loc>
    <lastmod>${new Date().toISOString()}</lastmod>
    <changefreq>${page.changefreq}</changefreq>
    <priority>${page.priority}</priority>
  </url>`).join('')}
</urlset>`
    
    return sitemap
  })

  // Sitemap produits
  fastify.get('/sitemap-products.xml', async (request, reply) => {
    reply.type('application/xml')
    
    const products = await Product.findAll({
      attributes: ['id', 'name', 'img', 'price', 'updatedAt']
    })

    const sitemap = `<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:image="http://www.google.com/schemas/sitemap-image/1.1">
${products.map(product => `
  <url>
    <loc>https://monshop.com/product/${product.id}</loc>
    <lastmod>${product.updatedAt.toISOString()}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>0.8</priority>
    ${product.img ? `
    <image:image>
      <image:loc>https://monshop.com/images/${product.img}</image:loc>
      <image:title>${product.name}</image:title>
      <image:caption>${product.name} - ${product.price}€</image:caption>
    </image:image>` : ''}
  </url>`).join('')}
</urlset>`
    
    return sitemap
  })

  console.log('✅ Routes SEO enregistrées')
}